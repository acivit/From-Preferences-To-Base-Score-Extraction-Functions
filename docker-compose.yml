version: '3.9'

services:
  bsef:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bsef
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    restart: "no"
    command: streamlit run show_base_scores.py
